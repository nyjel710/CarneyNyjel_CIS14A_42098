var price = 28.99;								// Crreate a variable named price and assign it the value of 28.99

var discount = 10;								// Create a variable named discount and assign it the value of 10

var total = price - (price * (dicount / 100));	// Compute new pricce by applying discount and assign it to total

if ( total > 25) {								// Compare value in Variable total to 25. If it is greater...

	freeShipping();								// ...then do something with freeShipping

}												// End of statement


var count = 10; 								// Create variable named count, assign it the value of 10

while (count > 0) {								// As long as the Variable count is greater than 0...

	juggle();									// ...Do some juggling, and...

	cout = count - 1; 							// ...reduce the value of count by 1 each time

} 												// End of while loop


var dog = {name: "Rover", weight: 35}; 			// Create a dog with a name and weight

if (dog.weight > 30) { 							// If the dog's weight is greater than 30...

	alert("WOOF WOOF"); 						// ...alert "WOOF WOOF" to browser's webpage

} else {										// otherwise...

	alert("woof woof");							// ...alert "woof woof" to the browser's webpage

}												// End of if/else statment


var circleRadius = 20; 							// Create a variable circleRadius and assign it the value 20

var calcArea = 									// Create a variable named calcArea...

Math.PI * (circleRadius * circleRadius) 		// ...and assigned the result of the expression to it
